[workspace]
resolver = "2"
[workspace.package]
license = "AGPL-3.0-only"

[package]
name = "deploy-graph"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ToadStool showcase: capability-based routing via deploy.capability_call"

# Local crate path: ../../../crates/core/common

[dependencies]
serde_json = "1"
colored = "2"
tokio = { version = "1", features = ["full"] }
