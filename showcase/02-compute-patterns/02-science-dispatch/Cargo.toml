[workspace]
resolver = "2"
[workspace.package]
license = "AGPL-3.0-or-later"

[package]
name = "science-dispatch"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ToadStool showcase: GPU compute job submission via science.* methods"

# Local crate path: ../../../crates/core/common

[dependencies]
serde_json = "1"
colored = "2"
uuid = "1"
tokio = { version = "1", features = ["full"] }
