[workspace]

[package]
name = "nestgate-artifact-storage"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "ToadStool showcase: NestGate artifact storage"

# Local crate path: ../../../crates/core/common

[dependencies]
serde_json = "1"
colored = "2"
tokio = { version = "1", features = ["full"] }
