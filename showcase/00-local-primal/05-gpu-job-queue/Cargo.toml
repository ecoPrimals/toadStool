[workspace]
resolver = "2"
[workspace.package]
license = "AGPL-3.0-or-later"

[package]
name = "gpu-job-queue"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "ToadStool showcase: GPU job queue submission, priority, and status tracking"

[dependencies]
toadstool-common = { path = "../../../crates/core/common" }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
colored = "2"
