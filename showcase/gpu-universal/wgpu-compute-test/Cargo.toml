[package]
name = "wgpu-compute-test"
version = "0.1.0"
edition = "2021"

[dependencies]
# EVOLVED: Use workspace wgpu config (Pure Rust! No renderdoc-sys) ✅
wgpu = { workspace = true }
pollster = "0.3"
bytemuck = { version = "1.14", features = ["derive"] }
anyhow = "1.0"
futures-intrusive = "0.5"

