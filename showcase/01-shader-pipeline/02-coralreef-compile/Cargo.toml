[workspace]

[package]
name = "coralreef-compile"
version = "0.1.0"
edition.workspace = true
license = "AGPL-3.0-only"
description = "ToadStool showcase: coralReef shader compilation with graceful fallback"

[dependencies]
serde_json = "1"
colored = "2"
tokio = { version = "1", features = ["full"] }
