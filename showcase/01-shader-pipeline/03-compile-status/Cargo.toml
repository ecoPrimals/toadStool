[workspace]

[package]
name = "compile-status"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "ToadStool showcase: async shader compilation status polling"

[dependencies]
serde_json = "1"
colored = "2"
uuid = { version = "1", features = ["v4"] }
tokio = { version = "1", features = ["full"] }
