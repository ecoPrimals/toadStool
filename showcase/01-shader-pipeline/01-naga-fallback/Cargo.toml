[workspace]

[package]
name = "naga-fallback"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "ToadStool showcase: WGSL shader compilation via naga fallback"

[dependencies]
serde_json = "1"
colored = "2"
tokio = { version = "1", features = ["full"] }
